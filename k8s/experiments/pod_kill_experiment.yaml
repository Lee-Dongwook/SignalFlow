apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kafka-pod-kill-test
  namespace: signalflow
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - signalflow
    labelSelectors:
      "app": "kafka"
  scheduler:
    cron: "*/5 * * * *"
